# Prek configuration for git hooks
# https://prek.j178.dev/

# Commands to run before each push
[[pre-push]]
name = "lint"
run = "make lint"

[[pre-push]]
name = "build-and-test"
run = "make build && make test"
